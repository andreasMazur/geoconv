This example requires further dependencies:

```bash
pip install torch_geometric torch_cluster fpsample matplotlib scipy pandas
```

In case no OpenGL context can't be created:
```bash
conda install -c conda-forge libstdcxx-ng
```
